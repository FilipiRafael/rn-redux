import { SafeAreaView } from 'react-native';
import Shop from './screens/shop';

const AppStartup = () => {
    return (
        <SafeAreaView>
            <Shop />
        </SafeAreaView>
    );
}

export default AppStartup;